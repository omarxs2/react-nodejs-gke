apiVersion: apps/v1
kind: Deployment
metadata:
  creationTimestamp: null
  labels:
    app: omar-react-webapp
  name: omar-react-webapp
spec:
  replicas: 5
  selector:
    matchLabels:
      app: react-webapp
  strategy: {}
  template:
    metadata:
      creationTimestamp: null
      labels:
        app: omar-react-webapp
    spec:
      containers:
      - image: gcr.io/staticweb-test/react-webapp:v1
        name: omar-webapp
        imagePullPolicy: Always
        resources: {}
        ports:
          - containerPort: 3080 
status: {}

---

apiVersion: v1
kind: Service
metadata:
  name: omar-react-webapp
  labels:
    run: omar-react-webapp
spec:
  ports:
  - port: 3080
    protocol: TCP
  selector:
    app: omar-react-webapp
  type: LoadBalancer